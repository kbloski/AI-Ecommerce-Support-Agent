# Projects

## Paths

Ścieżka poniżej jest względna wobec katalogu zawierającego ten plik (`agent-workspace/projects/`).

```text
../../
```

Czyli: `agent-workspace/projects/../../` = `E:\Projects\_\ai-ec-agent` (katalog nadrzędny wobec `agent-workspace`).
